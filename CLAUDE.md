# Stack
TypeScript (strict), Node 22, pnpm, vitest. ESM.

# Working agreement
- Propose the approach first. Wait for my OK before editing.
- Write the failing test before the implementation.
- Run `pnpm check` after every change. Don't tell me it works until it passes.
- No new dependency without asking. Prefer the standard library.
- No `any`. No `@ts-ignore`. If the types are hard, say so and explain why.
- Small diffs. One concern per change.
- When I ask "why", give me the tradeoff, don't just agree with me.
- Never commit, push, or create branches/worktrees unless I ask.